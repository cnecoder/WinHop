# TODO（未完成需求与改进）

## 功能缺口（已确认需求但未实现，Windows-only 后均为可用专属能力）

- [ ] **开机自启**（高优先级）：`autostart` 配置项，写入注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`（ADR-004 原定）
- [ ] **UIAccess 免 UAC**（高优先级）：代码签名 + 可信位置部署后可免提权弹窗（需签名证书）
- [ ] **虚拟桌面支持**（低优先级，用户场景少用）：目标窗口在另一任务视图桌面时无法激活——`IVirtualDesktopManager` COM API（先做识别+标记，再考虑自动切桌面）
- [ ] **`wintab capture` 辅助命令**：CLI 打印当前激活窗口的进程名/标题，方便手写配置（ADR-004 原定，现已被「+ 号一键入配置」部分替代，纯手写场景仍有用）

## 已知限制（待解决）

- [ ] **taskmgr 热键被吞**：taskmgr 自带 LL 键盘钩子抢链首，Ctrl+Space 失效（托盘图标兜底已实现）。可能的解法：周期重装钩子不可行（raw input 时代键盘钩子已废弃），需另想热键路径或接受现状
- [ ] **缩略图静态**：进入窗口层时捕获一次，不实时刷新。改进方向：选中窗口变化时按需刷新单张（需解决 PrintWindow 对遮挡窗口的稳定性）
- [ ] **部分窗口缩略图黑图**：全屏游戏/特殊硬件加速窗口 PrintWindow 可能返回黑（PW_RENDERFULLCONTENT 已用仍失败的场景）
- [ ] **IME 充分验证**：中文输入法（拼音）开着时选择页按键的完整兼容性测试

## 体验优化

- [ ] **实时缩略图**：进入窗口层时捕获一次为静态；改进方向——选中窗口变化时按需刷新单张（研究 DWM Thumbnail 像素读取的正确路径）
- [ ] **热键冲突检测**：`RegisterHotKey` 注册失败时提示占用方（目前静默失效）
- [ ] **配置界面编辑 programs**：目前「+ 号」只能添加不能删除/改字母；考虑设置面板内完整管理列表
- [ ] **主题跟随系统**：深色/浅色切换
- [ ] **DPI 感知**：多显示器不同缩放时缩略图/布局适配
- [ ] **窗口「拉到当前屏」选项**：每程序可配置切换时是否把窗口移到当前屏（原需求讨论过，默认留在原屏）
- [ ] **自动补全字母稳定性**：当前按进程名排序分配空闲字母，进程增减会重新分配；可考虑记住上次分配

## 工程

- [ ] **自动化测试**：目前无测试（状态机逻辑可抽离做单元测试；窗口枚举/激活需集成测试）
- [ ] **CI**：GitHub Actions 构建 release 产物并自动发布（当前手动打包上传）
- [ ] **日志轮转**：`%TEMP%\wintab.log` 无限增长，需按大小/时间轮转
